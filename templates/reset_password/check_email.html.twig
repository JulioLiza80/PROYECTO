{% extends 'base.html.twig' %}

{% block title %}Correo electrónico de restablecimiento de contraseña {% endblock %}

{% block body %}
	{{ include('comun/header.html.twig') }}
 <div class="flex items-center justify-center min-h-screen bg-gray-50">
        <div class="w-full max-w-md p-6 bg-white rounded-lg shadow-lg border border-gray-200 text-center">
            <div  class="text-lg text-gray-700 mb-6 space-y-6">
            <p>
                Si existe una cuenta que coincida con tu correo electrónico, se enviará un correo electrónico con un enlace que puedes usar para restablecer tu contraseña.
            </p>
            <p class="text-sm">
                Este enlace caducará en {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            </div>
            
            <p class="text-sm text-gray-600">
                Si no recibe un correo electrónico, revise su carpeta de correo no deseado o puedes <a href="{{ path('app_forgot_password_request') }}" class="text-blue-500 hover:text-blue-600">intentarlo otra vez</a>.
            </p>

        </div>
    </div>
	{{ include('comun/footer.html.twig') }}
{% endblock %}
