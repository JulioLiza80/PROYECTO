<form method="post" action="{{ path('app_gafas_delete', {'id': gafa.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ gafa.id) }}">
    <button class="btn">Delete</button>
</form>
